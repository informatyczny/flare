relay {
    bind = "0.0.0.0"
    port = 8080

    info {
        name = "FLARE City Events Mirror"
        description = "Curated mirror — registered FLARE volunteers only"
        contact = "you@yourdomain.com"
    }
}

db = "/app/strfry-db"

writePolicy {
    plugin = "/app/scripts/relay_policy.py"
}
